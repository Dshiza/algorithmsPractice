/**
 * @function latticeMultiplication
 * @description - The latticeMultiplication function is a large number multiplier with O(n*m) complexity
 * @param {string} num1
 * @param {string} num2
 * @returns {string}
 * @example - powLinear("2000", "3000") => 4 --> '0' * '0' + ... + '2' * '3'
 */
const latticeMultiplication = (num1, num2) => {
  let hold = 0;
  for (let k = 0; k < n + m; k++) {}
};

export { latticeMultiplication };
